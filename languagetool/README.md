# LanguageTool Container and Custom Rules Management API

## Overview

The LanguageTool Rule Management API is a lightweight Node.js service that:
- Updates `grammar.xml` files with custom rules from MongoDB
- Manages the LanguageTool Java process lifecycle
- Provides endpoints for grammar.xml updates and process management

All database operations are handled by the backend API.